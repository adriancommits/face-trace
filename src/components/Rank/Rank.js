import React from 'react';

const Rank = () => {
  const currentRank = 4;

  return (
    <p>
      You are currently ranked ${currentRank} worldwide. Detect more faces to
      rank up!
    </p>
  );
};

export default Rank;
